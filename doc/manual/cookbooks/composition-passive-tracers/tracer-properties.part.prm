subsection Postprocess
  subsection Tracers
    set Number of tracers        = 50000

    set List of tracer properties = function, initial composition, initial position, pT path

    subsection Function
      set Variable names      = x,y
      set Function expression = if(y<0.2, 1, 0)
    end
  end
end
